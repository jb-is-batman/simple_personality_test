name: personality_test
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  stacked: 2.3.3 # https://pub.dev/packages/stacked (State management architecture)
  get_it:  7.2.0 # https://pub.dev/packages/get_it (service locator)
  get:     4.6.1 # https://pub.dev/packages/get (navigation / widget display without context)
  logger:  1.1.0 # https://pub.dev/packages/logger (logging)

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: 0.3.0 # https://pub.dev/packages/mocktail (Unit testing)

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
